// Site configuration
// Set `employeeLoginUrl` to your real employee login / SSO URL to bypass the demo modal.
// Example: window.SITE_CONFIG = { employeeLoginUrl: 'https://intranet.example.com/login' };
window.SITE_CONFIG = window.SITE_CONFIG || { employeeLoginUrl: '' };
